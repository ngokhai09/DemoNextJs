const houseTypeReducer = (state: any, action: any)=>{
    switch (action.type){
        case "FETCH_DATA":
            return state;
        default: return state;
    }
};
export default houseTypeReducer;